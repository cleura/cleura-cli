## What & why

<!-- What does this change, and why? Link any related issue. -->

## Checklist

- [ ] `go test ./...`, `go vet ./...`, and `make build` pass
- [ ] `make docs` re-run if any command or flag changed (regenerated `docs/reference/` committed)
- [ ] No secrets, tokens, or personal paths in the diff
